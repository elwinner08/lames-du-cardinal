{{!-- FIGURANT HEADER --}}
<header class="sheet-header">
  <img class="profile-img" src="{{actorImg}}"
       alt="{{actor.name}}" title="{{actor.name}}" />
  <div class="header-fields">
    <h1 class="actor-name"><input type="text" name="name" value="{{actor.name}}" /></h1>
    <div class="header-details">
      <span class="figurant-profil">{{system.profil}}</span>
      <span>{{localize (concat "LAMES.FigurantTypes." system.typeFigurant)}}</span>
      <span>{{localize (concat "LAMES.Figurant." system.modificateur)}}</span>
    </div>
    <div class="header-combat-resume">
      {{resumeCombat}}
      {{#if horsCombat}}<span class="hors-combat">{{localize "LAMES.Figurant.horsCombat"}}</span>{{/if}}
    </div>
  </div>
</header>
